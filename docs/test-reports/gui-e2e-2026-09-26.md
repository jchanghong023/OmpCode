# GUI 端到端测试记录（2026-09-26）

## 范围与环境

- 使用隔离的本地 Web 服务和后台浏览器，不切换用户正在观看的视频窗口。
- Web GUI 中选择 `zhipu-coding-plan/GLM-5.3-Flash`，发送“GUI 端到端测试：请只回复 OK，不调用工具。”；时间线显示模型已切换，回复为 `OK`，运行结束。
- 在 390 × 844 视口检查手机聊天布局，消息、回复、输入框及模型选择器可见；测试后已恢复默认视口。
- Electron 桌面实例的后台启动被自动审批拒绝，返回 `blocked by policy`，桌面专属界面未验收。

## 已检查的 Web 界面

| 区域     | 检查结果                                                                                                           |
| -------- | ------------------------------------------------------------------------------------------------------------------ |
| 引导页   | 可进入、切换步骤和退出。                                                                                           |
| 主聊天   | 模型选择、真实回复、文件提及搜索、斜杠命令列表和上下文控件可操作。                                                 |
| 侧栏     | 项目与分组、筛选排序、归档空状态、显示效果与帮助菜单可打开。                                                       |
| 命令面板 | 全部、操作、任务、文件四个分类可切换。                                                                             |
| 自动化   | 列表空状态、新建表单、每日规则及历史空状态可显示；未创建计划。                                                     |
| 设置     | 常规、外观、模型、浏览器控制、快捷键、记忆、子智能体、插件、MCP 服务器、技能、命令、钩子、使用统计、引导均可进入。 |
| 侧面板   | 终端打开并执行只读 `pwd`；审查空状态可显示。                                                                       |
| 工作流   | 界面显示 `method not supported by omp core: workflows/list`，与 Fork 已记录的能力差异一致。                        |

## 待处理发现

### 辅助对话标签无法创建

1. 打开 Web 主聊天及右侧面板。
2. 在“新增标签”菜单点击“辅助对话”。
3. 没有出现新的辅助对话标签；控制台记录 `[ui] [v4-pane] 创建框选副屏会话失败 Object`。

此问题在隔离 Web 测试环境复现；原因及桌面环境是否同样受影响尚未确认。

## 验证边界

- 本次走查覆盖可到达的 Web 顶层界面和一条真实 GLM-5.3-Flash 对话，不等于每个按钮、设置提交或桌面原生功能均已验收。
- 浏览器为后台运行，未干扰用户视频；测试进程已经关闭。
